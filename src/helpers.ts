export function concat2(str1: string, str2: string): string {
  return str1 + '-' + str2;
}

export function concat3(str1: string, str2: string, str3: string): string {
  return str1 + '-' + str2 + '-' + str3;
}